{
  "id": "12-extension-exclusion",
  "description": "Extension exclusion — the base record must NOT contain Construction, Build Archetypes, RS05, Scale V, or any field-dependent claim. These are optional SignalAF reference extensions.",
  "input": {
    "telemetry": { "input": 1000, "output": 5000, "cache_write": 500, "cache_read": 3000 },
    "source": { "provider": "test", "model": "test", "tool": "test" }
  },
  "expected": {
    "forbidden_metrics": ["construction", "archetype", "rs05", "scale_v", "scaleV", "class", "rank", "percentile"],
    "required_metrics": ["yield", "leverage", "velocity", "snr", "dev10x"]
  }
}
