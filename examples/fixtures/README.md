# examples/fixtures/ (SUPERSEDED)

> **⚠️ SUPERSEDED:** This directory contains the original `sigrank/0.1-draft`
> test fixtures (13 JSON files using legacy metric names `snr` and `dev10x`).
> They have been superseded by the OTEP test vectors in
> [`../../test-vectors/`](../../test-vectors/).
>
> The OTEP test vectors use the normative metric names:
> - `output_fraction` (legacy alias: `snr`)
> - `log_leverage` (legacy alias: `dev10x`)
>
> And the OTEP protocol version string `otep/0.1-draft`.
>
> These legacy fixtures are retained for backward compatibility with the
> original `sigrank/0.1-draft` conformance runner. The authoritative OTEP
> conformance runners (JS: `conformance/otep-runner.mjs`, Python:
> `python/sigrank_standard/conformance.py`) use `test-vectors/`, not this
> directory.
>
> Do not use these fixtures for new OTEP-conformant implementations.
