{
  "id": "10-version-declaration",
  "description": "Version declaration — the record must declare spec: 'sigrank/0.1-draft'. Any other value is non-conformant.",
  "input": {
    "telemetry": { "input": 1000, "output": 5000, "cache_write": 500, "cache_read": 3000 },
    "source": { "provider": "test", "model": "test", "tool": "test" }
  },
  "expected": {
    "spec": "sigrank/0.1-draft"
  }
}
